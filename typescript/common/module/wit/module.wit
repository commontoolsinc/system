package common:module@0.0.1;

interface module {
  resource body {
    // Invoke the module's substantive procedure
    run: func();
  }

  // Instantiate the module
  create: func() -> body;
}

world common {
  import common:data/types@0.0.1;
  import common:io/state@0.0.1;

  export module;
}