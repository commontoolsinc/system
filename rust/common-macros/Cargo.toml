[package]
name = "common-macros"
description = "Macros for common crates."
version = "0.1.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

[dependencies]
quote = { workspace = true }
syn = { workspace = true }

[features]
tracing = []
